<%= render :partial => "/list" %>
<p>目標</p>
<%#= form_with model: @user, url: user_path(@user), local: true do |f| %>
<%#= f.text_field :goal %>
<%# end%>

<%= form_with model: @user, url: user_path(@user), local: true do |f| %>
<div style="text-align:center;">
  <%= f.text_area :goal, size: "100x5"%>
</div>

<div class="mt-3", style="text-align:center;">
  <%= f.submit "次へ", class:'btn btn-danger' %>
</div>
  
<% end %>